diff --git a/open-sse/config/providerRegistry.ts b/open-sse/config/providerRegistry.ts
index 6a2e82a6..6ed630e7 100644
--- a/open-sse/config/providerRegistry.ts
+++ b/open-sse/config/providerRegistry.ts
@@ -889,7 +889,6 @@ export const REGISTRY: Record<string, RegistryEntry> = {
       {
         id: "claude-haiku-4.5",
         name: "Claude Haiku 4.5",
-        targetFormat: "openai-responses",
         contextLength: 200000,
         maxOutputTokens: 64000,
       },
@@ -928,8 +927,8 @@ export const REGISTRY: Record<string, RegistryEntry> = {
         contextLength: 1000000,
         maxOutputTokens: 128000,
       },
-      { id: "gemini-3.1-pro-preview", name: "Gemini 3.1 Pro", targetFormat: "openai-responses" },
-      { id: "gemini-3-flash-preview", name: "Gemini 3 Flash", targetFormat: "openai-responses" },
+      { id: "gemini-3.1-pro-preview", name: "Gemini 3.1 Pro" },
+      { id: "gemini-3-flash-preview", name: "Gemini 3 Flash" },
       { id: "oswe-vscode-prime", name: "Raptor Mini", targetFormat: "openai-responses" },
       //{ id: "?", name: "Goldeneye" },
     ],
diff --git a/open-sse/executors/base.ts b/open-sse/executors/base.ts
index 6fa9f258..844fb0e5 100644
--- a/open-sse/executors/base.ts
+++ b/open-sse/executors/base.ts
@@ -838,7 +838,7 @@ export class BaseExecutor {
           const ccHeaders: Record<string, string> = {
             Accept: "application/json",
             "anthropic-version": "2023-06-01",
-            "anthropic-beta": selectBetaFlags(tb),
+            "anthropic-beta": selectBetaFlags(tb, model),
             "anthropic-dangerous-direct-browser-access": "true",
             "x-app": "cli",
             "User-Agent": `claude-cli/${CLAUDE_CODE_VERSION} (external, cli)`,
diff --git a/open-sse/executors/claudeIdentity.ts b/open-sse/executors/claudeIdentity.ts
index 30e081ac..9fc4503a 100644
--- a/open-sse/executors/claudeIdentity.ts
+++ b/open-sse/executors/claudeIdentity.ts
@@ -289,7 +289,16 @@ export function parseUpstreamMetadataUserId(
  * uses three patterns: minimal probe, structured-output, and full agent.
  * Sending the full set on every shape is itself a fingerprint.
  */
-export function selectBetaFlags(body: Record<string, unknown> | null | undefined): string {
+const MODELS_WITHOUT_LONG_CONTEXT = new Set([
+  "claude-haiku-4-5-20251001",
+  "claude-haiku-4.5",
+  "claude-haiku-4-5",
+]);
+
+export function selectBetaFlags(
+  body: Record<string, unknown> | null | undefined,
+  model?: string | null
+): string {
   const b = body || {};
   const hasSystem =
     !!b.system &&
@@ -302,10 +311,12 @@ export function selectBetaFlags(body: Record<string, unknown> | null | undefined
     !!(b.response_format as { type?: string } | undefined)?.type;
   const isFullAgent = hasTools && hasSystem;
 
+  const isHaiku = typeof model === "string" && MODELS_WITHOUT_LONG_CONTEXT.has(model);
+
   const flags: string[] = [];
   if (isFullAgent) flags.push("claude-code-20250219");
   flags.push("oauth-2025-04-20");
-  if (isFullAgent) flags.push("context-1m-2025-08-07");
+  if (isFullAgent && !isHaiku) flags.push("context-1m-2025-08-07");
   flags.push(
     "interleaved-thinking-2025-05-14",
     "redact-thinking-2026-02-12",
