diff --git a/client/src/components/Chat/Messages/SearchMessage.tsx b/client/src/components/Chat/Messages/SearchMessage.tsx
index 982aee06c..a7b2c5c21 100644
--- a/client/src/components/Chat/Messages/SearchMessage.tsx
+++ b/client/src/components/Chat/Messages/SearchMessage.tsx
@@ -1,4 +1,4 @@
-import { useMemo } from 'react';
+import { useEffect, useMemo, useRef } from 'react';
 import { useAtomValue } from 'jotai';
 import { useRecoilValue } from 'recoil';
 import { useAuthContext, useLocalize } from '~/hooks';
@@ -35,11 +35,15 @@ const MessageBody = ({ message, messageLabel, fontSize }) => (
   </div>
 );
 
-export default function SearchMessage({ message }: Pick<TMessageProps, 'message'>) {
+export default function SearchMessage({
+  message,
+  isActive = false,
+}: Pick<TMessageProps, 'message'> & { isActive?: boolean }) {
   const fontSize = useAtomValue(fontSizeAtom);
   const UsernameDisplay = useRecoilValue<boolean>(store.UsernameDisplay);
   const { user } = useAuthContext();
   const localize = useLocalize();
+  const itemRef = useRef<HTMLDivElement>(null);
 
   const iconData: TMessageIcon = useMemo(
     () => ({
@@ -67,12 +71,25 @@ export default function SearchMessage({ message }: Pick<TMessageProps, 'message'
     localize,
   ]);
 
+  useEffect(() => {
+    if (isActive && itemRef.current) {
+      itemRef.current.scrollIntoView({ behavior: 'smooth', block: 'center' });
+    }
+  }, [isActive]);
+
   if (!message) {
     return null;
   }
 
   return (
-    <div className="text-token-text-primary w-full bg-transparent">
+    <div
+      ref={itemRef}
+      className={cn(
+        'text-token-text-primary w-full bg-transparent transition-colors duration-200',
+        isActive &&
+          'border-l-2 border-blue-500 bg-blue-50/80 dark:border-blue-400 dark:bg-blue-900/20',
+      )}
+    >
       <div className="m-auto p-4 py-2 md:gap-6">
         <div className="final-completion group mx-auto flex flex-1 gap-3 md:max-w-3xl md:px-5 lg:max-w-[40rem] lg:px-1 xl:max-w-[48rem] xl:px-5">
           <MessageAvatar iconData={iconData} />
diff --git a/client/src/routes/Search.tsx b/client/src/routes/Search.tsx
index b4db28c65..a92eaf187 100644
--- a/client/src/routes/Search.tsx
+++ b/client/src/routes/Search.tsx
@@ -1,4 +1,4 @@
-import { useEffect, useMemo } from 'react';
+import { useCallback, useEffect, useMemo, useState } from 'react';
 import { useRecoilValue } from 'recoil';
 import { Spinner, useToastContext } from '@librechat/client';
 import MinimalMessagesWrapper from '~/components/Chat/Messages/MinimalMessages';
@@ -15,6 +15,7 @@ export default function Search() {
   const { isAuthenticated } = useAuthContext();
   const search = useRecoilValue(store.search);
   const searchQuery = search.debouncedQuery;
+  const [activeIndex, setActiveIndex] = useState(0);
 
   const {
     data: searchMessages,
@@ -64,6 +65,45 @@ export default function Search() {
     }
   }, [isError, searchQuery, showToast]);
 
+  useEffect(() => {
+    setActiveIndex(0);
+  }, [messages]);
+
+  const handleKeyDown = useCallback(
+    (e: KeyboardEvent) => {
+      if (!messages || messages.length === 0) {
+        return;
+      }
+      const target = e.target as HTMLElement;
+      if (
+        target.tagName === 'INPUT' ||
+        target.tagName === 'TEXTAREA' ||
+        target.isContentEditable
+      ) {
+        return;
+      }
+      if (e.code === 'PageDown') {
+        e.preventDefault();
+        setActiveIndex((prev) => Math.min(prev + 1, messages.length - 1));
+      } else if (e.code === 'PageUp') {
+        e.preventDefault();
+        setActiveIndex((prev) => Math.max(prev - 1, 0));
+      } else if (e.code === 'Home') {
+        e.preventDefault();
+        setActiveIndex(0);
+      } else if (e.code === 'End') {
+        e.preventDefault();
+        setActiveIndex(messages.length - 1);
+      }
+    },
+    [messages],
+  );
+
+  useEffect(() => {
+    document.addEventListener('keydown', handleKeyDown);
+    return () => document.removeEventListener('keydown', handleKeyDown);
+  }, [handleKeyDown]);
+
   const resultsCount = messages?.length ?? 0;
   const resultsAnnouncement = useMemo(() => {
     if (resultsCount === 0) {
@@ -102,9 +142,20 @@ export default function Search() {
         </div>
       ) : (
         <>
-          {messages.map((msg) => (
-            <SearchMessage key={msg.messageId} message={msg} />
+          {messages.map((msg, index) => (
+            <SearchMessage
+              key={msg.messageId}
+              message={msg}
+              isActive={index === activeIndex}
+            />
           ))}
+          {resultsCount > 1 && (
+            <div className="pointer-events-none sticky bottom-2 flex justify-center">
+              <div className="rounded-full bg-gray-200/80 px-3 py-1 text-xs text-gray-600 dark:bg-gray-700/80 dark:text-gray-300">
+                {activeIndex + 1} / {resultsCount}
+              </div>
+            </div>
+          )}
           {isFetchingNextPage && (
             <div className="flex justify-center py-4">
               <Spinner className="text-text-primary" />
