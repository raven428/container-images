# kics-scan disable=555ab8f9-2001-455e-a077-f2d0f41e2fb9
---
name: containers
on:
  push:
    branches:
      - master
  pull_request:
  schedule:
    - cron: "1 1 * * 4"
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  define-matrix:
    name: define matrix
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.define-matrix.outputs.matrix }}
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          submodules: true
      - name: define matrix
        id: define-matrix
        env:
          GITHUB_EVENT_BEFORE: ${{ github.event.before }}
        run: |
          source _shared/_all/matrix.sh
          echo "matrix=${PKG_MATRIX}" >> "${GITHUB_OUTPUT}"
  containers:
    name: "${{ matrix.kind }} ${{ matrix.tag }}"
    needs: define-matrix
    if: needs.define-matrix.outputs.matrix != '{"include":[]}'
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.define-matrix.outputs.matrix) }}
    timeout-minutes: 22
    runs-on: ubuntu-24.04
    permissions:
      packages: write
    env:
      VERSION_SUFFIX: ${{ format('{0}.{1}', github.run_number, github.run_attempt) }}
      PUBLISH_MODE: "${{ github.event_name == 'schedule' && 'schedule' ||
        github.event.pull_request.head.repo.full_name == github.repository &&
        github.event_name == 'push' && 'release' || github.event_name == 'pull_request' &&
        github.event.pull_request.user.login != 'dependabot[bot]' && 'dev' || 'skip' }}"
    steps:
      - name: checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          fetch-tags: true
          submodules: recursive
      - name: build and test images
        run: |
          sudo ./podman.sh
          MANUAL_IMAGES_DIRS='${{ matrix.tag }}/' _shared/_all/build.sh
      - name: push images
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [[ '${{ matrix.kind }}' == 'img' ]]; then
            echo 'logging in to ghcr for container package'
            printf '%s' "${NODE_AUTH_TOKEN}" |
              podman login ghcr.io --username '${{ github.actor }}' --password-stdin
          else
            echo 'skipping ghcr login for npm package'
          fi
          MANUAL_IMAGES_DIRS='${{ matrix.tag }}/' _shared/_all/push.sh
  MegaLinter:
    name: MegaLinter
    timeout-minutes: 11
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          submodules: true
      - name: MegaLinter
        uses: oxsecurity/megalinter@v9
        env:
          VALIDATE_ALL_CODEBASE: true
  all-green:
    name: all green
    timeout-minutes: 3
    runs-on: ubuntu-latest
    needs:
      - containers
      - MegaLinter
    if: github.event_name == 'pull_request'
    permissions:
      checks: read
    steps:
      - name: waiting for checks success
        uses: poseidon/wait-for-status-checks@v0.6.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          ignore: all green, foobar
          timeout: 111s
